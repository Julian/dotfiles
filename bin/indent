#!/usr/bin/env python3
import sys

if len(sys.argv) == 1:
    spaces = 4 * " "
else:
    spaces = int(sys.argv[1]) * " "

for line in sys.stdin:
    sys.stdout.write(spaces)
    sys.stdout.write(line)
